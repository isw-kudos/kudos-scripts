{
  "name": "kudos-scripts",
  "version": "1.0.0",
  "author": "ISW",
  "description": "common scripts for kudos microservices",
  "bin": {
    "deps": "./build.deps",
    "updeps": "./git-deps.update",
    "migrate": "./git-deps.migrate"
  }
}
